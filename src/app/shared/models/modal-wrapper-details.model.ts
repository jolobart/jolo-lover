import { ModalTitle } from '../constants/modal-title.constant';
import { ComponentType } from '../enums';

export interface ModalWrapperDetails {
  title: ModalTitle;
  componentType: ComponentType;
}
